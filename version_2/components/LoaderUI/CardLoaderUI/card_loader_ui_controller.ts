import BaseController from "../../../base_classes/base_controller";

class CardLoaderUIController extends BaseController {
    constructor(props: Record<string, any> = {}) {
        super("card_loader_ui", props);
    }
}

export default CardLoaderUIController;
